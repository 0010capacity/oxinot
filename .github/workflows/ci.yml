name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  RUST_BACKTRACE: 1

jobs:
  check-changeset-only:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.changeset_check.outputs.skip }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if changeset-only PR
        id: changeset_check
        if: github.event_name == 'pull_request'
        run: |
          FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD)
          NON_CHANGESET_FILES=$(echo "$FILES" | grep -v "^\.changeset/" || true)
          if [ -z "$NON_CHANGESET_FILES" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "ℹ️ Changeset-only PR detected, skipping CI checks"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

  lint-and-build:
    needs: check-changeset-only
    if: needs.check-changeset-only.outputs.skip != 'true'
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: "npm"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri

      - name: Install dependencies
        run: npm install

      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Build frontend
        run: npm run build

      - name: Build Tauri (check only)
        run: npm run tauri:build
        continue-on-error: true
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Generate changeset
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npm run changeset:auto
          git add .changeset
          if ! git diff --staged --quiet; then
            CHANGESET_FILE=$(git diff --staged --name-only | grep ".changeset/" | head -1)
            CHANGESET_NAME=$(basename "$CHANGESET_FILE" .md)
            BRANCH_NAME="changeset/$CHANGESET_NAME"

            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            # Fetch latest main and rebase to ensure we're up-to-date
            git fetch origin main
            git rebase origin/main

            # Check if changeset branch already exists remotely and delete if it does
            if git rev-parse --verify "origin/$BRANCH_NAME" >/dev/null 2>&1; then
              echo "ℹ️ Changeset branch $BRANCH_NAME already exists, skipping..."
              exit 0
            fi

            git checkout -b "$BRANCH_NAME"
            git add .changeset
            git commit -m "chore: auto-generate changeset"
            git push origin "$BRANCH_NAME"

            PR_URL=$(gh pr create \
              --title "chore: auto-generate changeset" \
              --body "Automatically generated changeset from merged PR." \
              --head "$BRANCH_NAME")

            PR_NUMBER=$(echo "$PR_URL" | grep -oP '\d+$')

            # Enable auto-merge
            gh pr merge "$PR_NUMBER" --auto --squash

            echo "✅ Changeset PR created and auto-merge enabled: $PR_URL"
          else
            echo "ℹ️ No changeset generated (no qualifying commits)"
          fi
