Loaded cached credentials.
## 🔒 보안 및 인프라 리뷰

### ⚠️ 심각도 높음 (High Priority)

**[src/tauri-api.ts:4-47] 클라이언트 측 검증 의존 및 불완전한 경로 제어**
파일 시스템 작업(`read_file`, `write_file` 등)에 대한 검증 로직(`validatePath`)이 TypeScript(Frontend)에만 구현되어 있습니다. Tauri 아키텍처에서 렌더러 프로세스가 침해당할 경우(예: XSS), 공격자는 `tauriAPI` 래퍼를 거치지 않고 직접 `invoke`를 호출하여 검증을 우회할 수 있습니다. 또한, `path.includes("..")` 검사는 절대 경로(예: `/etc/passwd`, `C:\Windows\System32\...`) 접근을 막지 못합니다.
**해결 방법:**
1. Rust Backend(Command 핸들러)에서 모든 파일 경로가 허용된 'Workspace' 디렉토리 내부인지 확인하는 로직을 필수적으로 구현하세요. (Path Canonicalization 사용)
2. 절대 경로 입력을 차단하거나, Workspace 루트 기준으로 상대 경로만 허용하도록 변경하세요.
3. 클라이언트 측 검증은 UX 용도로만 유지하고, 보안 통제는 Backend로 이관하세요.

**[src/tauri-api.ts:133] 임의 파일 쓰기 및 삭제 위험**
`writeFile`, `deletePath` 등의 함수가 특정 디렉토리로 격리(Sandboxing)되지 않은 채 파일 시스템에 접근하는 것으로 보입니다. 이는 랜섬웨어 공격이나 시스템 파일 훼손 취약점으로 이어질 수 있습니다.
**해결 방법:** Tauri의 [fs scope](https://tauri.app/v1/api/config/#securityconfig) 기능을 활용하여 앱이 접근 가능한 경로를 화이트리스트로 엄격히 제한하거나, Rust 코드 내에서 `chroot`와 유사한 논리적 격리를 구현하세요.

### ⚡ 심각도 중간 (Medium Priority)

**[src-tauri/tauri.conf.json:80] CSP 'unsafe-inline' 스타일 허용**
`style-src 'self' 'unsafe-inline'` 설정은 인라인 스타일 주입을 허용합니다. 이는 마크다운 에디터 특성상 필요할 수 있으나, CSS를 통한 데이터 유출 등의 공격 벡터가 될 수 있습니다.
**해결 방법:** 가능하다면 `unsafe-inline`을 제거하고 고정된 스타일시트나 Nonce 기반 정책을 적용하세요. 불가피하다면 스타일 처리가 격리된 샌드박스 내에서 이루어지는지 검토하세요.

**[src-tauri/capabilities/default.json:16] 광범위한 Shell Open 권한**
`shell:allow-open` 권한이 부여되어 있습니다. 검증되지 않은 URL이나 파일을 열도록 유도할 경우 피싱이나 악성 스크립트 실행 위험이 있습니다.
**해결 방법:** `shell:scope` 설정을 통해 열 수 있는 URL 스키마(예: `https:`, `mailto:`)를 명시적으로 제한하거나, `open` 명령 호출 전 Rust 측에서 엄격한 정규식 검증을 수행하세요.

### 💡 기존 설정 개선 제안 (Configuration Improvements)

**1. Tauri FS Scope 명시적 적용**
Tauri의 기본 보안 기능을 활용하여 접근 가능한 경로를 설정 파일 레벨에서 제한하는 것이 좋습니다.

*   **Before (현재 - 암시적/커스텀 처리):**
    `tauri.conf.json`의 `plugins > fs`가 비어 있고, Custom Command 사용 중.

*   **After (제안):**
    ```json
    // src-tauri/tauri.conf.json
    "app": {
      "security": {
        "csp": "...",
        "capabilities": ["default"] // Tauri v2 스타일 권한 관리
      }
    }
    // capabilities 파일 내에서 scope 정의 (Tauri 버전에 따라 다름)
    ```
    *참고: 현재 코드가 Custom Command(`invoke`)를 주로 사용하므로, Rust 코드 내 경로 검증이 더 중요할 수 있습니다.*

# 청크 정보
청크 번호: 1/1
파일 목록:
- src-tauri/tauri.conf.json
- src-tauri/capabilities/default.json
- src/tauri-api.ts
